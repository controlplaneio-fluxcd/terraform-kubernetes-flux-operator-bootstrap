apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    flux-operator-bootstrap/revision: {{ .Values.revision | quote }}
{{- range $key, $value := .Values.job.serviceAccountAnnotations }}
    {{ $key }}: {{ $value | quote }}
{{- end }}
